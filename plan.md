# WeFinally AI 匹配与合伙人能力实施 Prompt

继续开发 WeFinally 微信小程序，严格按照本计划执行。本文件是本轮实现范围、顺序、验收标准和停止条件的唯一执行入口；不得把历史归档计划当成当前需求真源。

## 1. 工作目录与 Git 规则

唯一工作目录：

```text
D:\wefinal\.worktrees\wefinally-ai-agent
```

开始前必须：

1. 完整阅读根目录 `AGENTS.md`、`PROJECT_HANDOFF.md`、`CONTRIBUTING.md`、`project-docs/NEXT_THREAD_HANDOFF_2026-08-12_AI_MATCHING_PARTNER_DASHBOARD.md` 和本文件。
2. 执行 `git status --short --branch`，记录当前分支、HEAD 和已有改动。
3. 所有开始前已经存在或并发出现的修改均视为用户所有。
4. 禁止对用户改动执行 `reset`、`clean`、`checkout`、`restore`、覆盖、删除或顺手整理。
5. 不得在外层 `D:\wefinal` 主目录开发。
6. 每个会修改文件的实现批次必须先测试、审查最终 diff，只暂存本批文件或 hunk，并创建一个简洁 Git commit。
7. 不 push、不 amend、不变基、不改写历史、不自动合并或删除分支。
8. 最终报告分支名、commit hash、测试结果、未解决阻塞和仍未提交的用户改动。

## 2. 本轮总目标

在复用现有页面、组件、业务服务和 CloudBase 架构的前提下完成：

1. “其他补充需求”和 AI 意图画像。
2. 确定性硬筛、双向评分、脱敏 Top-K、DeepSeek 双向语义重排和双方独立报告。
3. 所有正式账号只能成功匹配一次，并保证并发安全。
4. 匹配详情显示归一化“综合匹配 XX 分”和一致的进度条。
5. 同一小程序中的普通会员/合伙人角色化界面。
6. 合伙人一键分享、备用邀请码、邀请归因和单页 Dashboard。
7. 微信官方人工客服入口；用户本人后续在微信后台绑定为客服人员。
8. 本地小程序、云函数模拟、管理后台和合伙人后台的相称验证。

本轮不开发：

- 营销自动化、群发推广和复杂运营 CRM；
- 自动提现和复杂财务审批；
- 纯 RAG 匹配；
- 为匹配功能强行引入 LangGraph；
- 虚假真人资料池或未经授权抓取婚恋网站数据；
- AI 扫描全库、直接写数据库或接触手机号、OpenID、微信号、精确地址、密钥、私钥。

## 3. 产品角色

只使用一个微信小程序和一个代码库，不创建两个 AppID。

- 普通会员：资料、其他补充需求、AI 匹配、匹配详情、客服。
- 合伙人：拥有普通会员能力，并额外看到邀请分享和合伙人 Dashboard。
- 普通用户前端不渲染合伙人入口；后端同时执行角色鉴权，不能只靠隐藏按钮。
- 合伙人只能读取自己的邀请、佣金和聚合数据。

## 4. 其他补充需求与 AI 意图画像

在现有匹配需求表单最底部增加一个选填的“其他补充需求”多行文本框，建议最多 500 字。不要再单独增加“期望地区”字段；城市、未来生活地区、家庭与事业规划、三观、生活方式、外貌气质偏好及其他未覆盖要求均可写在该文本框中。

要求：

- 通过白名单业务服务保存到正式匹配设置；
- 进入版本化匹配快照和审计；
- 原文不直接展示给另一方；
- 发送给模型前进行脱敏、限长和安全检查；
- AI 不得把猜测写成用户明确要求。

增加“AI 意图编译器”，将结构化资料和补充文字转换为严格 JSON，至少包含：

```text
must_have
preferences
values
lifestyle
appearance_preferences
deal_breakers
uncertainties
contradictions
clarification_questions
evidence
confidence
profile_confidence
prompt_version
```

每个推断必须保留脱敏证据、强度和置信度。AI 可以理解用户自述的外貌和气质偏好，但不得给照片打颜值分，也不得从照片推断性格、健康、经济或其他敏感属性。

### 4.1 两种交互模式

两种模式共用同一套后端、Schema 和 AI 服务，通过配置切换，不复制两套业务逻辑：

```text
AI_INTENT_CONFIRMATION_MODE=automatic
AI_INTENT_CONFIRMATION_MODE=confirm
```

模式 A：无感自动版

- 提交资料后后台自动生成画像；
- 默认不增加确认页面；
- 只有严重歧义、矛盾或关键资料不足时才追问。

模式 B：AI 确认版

- 展示“我对你的理解”确认卡；
- 展示必要条件、重要偏好、三观、生活方式、外貌气质偏好和待确认项；
- 用户可确认、修改单项或补充说明；
- 资料未变化时不重复确认。

两个模式均须本地可运行、可切换、可测试，供老板比较操作复杂度后再决定生产默认值。

## 5. AI 匹配主链

按以下顺序实现，不得让模型替代确定性业务边界：

```text
结构化资料 + 其他补充需求
→ 内容安全与隐私检查
→ AI 意图画像
→ 确定性硬条件筛选
→ 双向结构化评分和质量门槛
→ 取得脱敏 Top-K
→ DeepSeek 双向语义理解与重排
→ 后端复核
→ 原子占用双方唯一匹配资格
→ 生成双方独立报告
→ 正式送达
```

Top-K 指合格候选池中结构化双向评分排名靠前的 K 人，不是最终匹配结果。若通过硬筛的人数少于 K，则使用全部合格候选；若为 0，则明确结束且不消耗匹配资格。

### 5.1 硬条件

由确定性代码处理：性别、年龄、婚姻状态、用户明确标记为必须的身高或城市约束、婚育/吸烟等不可接受项、账号安全状态、审核状态和是否已成功匹配。AI 不得恢复硬筛淘汰者。

### 5.2 双向结构化评分

必须分别计算 A 对 B 和 B 对 A 的满足程度。最终排序突出双向短板，可使用最低边约束或调和平均，避免一方高分掩盖另一方明显低分。

### 5.3 DeepSeek 双向语义重排

DeepSeek 只能读取硬筛后的少量脱敏候选摘要，重点理解：三观、择偶观、沟通与冲突处理、家庭和事业规划、地区与未来生活规划、生活方式、用户自述的外貌气质偏好、补充需求、共同满足点、单向不对称、冲突和待确认问题。

返回严格 JSON：

```text
candidate_ref
a_to_b_semantic_score
b_to_a_semantic_score
mutual_semantic_score
mutual_strengths
asymmetric_risks
confirmation_questions
evidence_tags
data_completeness
confidence
prompt_version
```

模型返回的 `candidate_ref` 必须映射到服务端安全引用，不能接受任意用户 ID。AI 只在有限权重内调整排序，不能覆盖硬筛、安全、权限、质量门槛和唯一匹配限制。AI 最多重试一次；仍超时、格式错误或低置信度时回退到确定性排序。如果自由文本包含决定性条件且无法可靠理解，则停止匹配，不强行生成结果。

模型不得直接写数据库。所有写入必须经过身份校验、字段白名单、幂等、权限和审计齐全的业务服务。

## 6. 所有正式账号只能成功匹配一次

这是正式产品规则，不是内测限制：

- 每个真实账号一生最多参与一条成功生成并正式送达的匹配关系；
- A 匹配到 B 后，A、B 同时被占用，之后均不能发起匹配或成为其他人的候选；
- 无候选、模型失败、超时或正式结果未写入不消耗资格；
- 测试夹具和明确隔离的系统测试账号不消耗真实用户资格。

后端必须原子执行：

1. 检查 A、B 均未匹配；
2. 建立双方唯一 match claim/lock；
3. 使用规范化 `pair_key` 和幂等请求号写入唯一关系；
4. 更新双方状态并写审计；
5. 任一步失败则不留下半完成占用；
6. 并发请求只能有一个成功，其余尝试下一个仍达标候选。

不得依赖前端禁用按钮。普通用户和普通运营人员不得重新匹配。只有超级管理员可针对确认的系统错误执行有原因、操作者、时间、原关系和幂等记录的审计纠错。

## 7. 综合分与报告

匹配详情同时显示“综合匹配 XX 分”、相同归一化值的进度条、A 对 B、B 对 A、共同满足点、三观和生活规划、潜在不对称、待确认问题、数据完整度、AI 置信度和报告版本。

报告必须有证据，不能只输出通用讨好文案。双方报告分别生成，不泄露另一方原始隐私要求。

历史分数读取优先级：

```text
score_detail.normalized_total
→ score_detail.normalizedTotal
→ total / max_total × 100
→ 旧版百分制 total_score
```

禁止把 128 分制原始 `total_score` 直接当百分制。

## 8. 合伙人分享、归因与 Dashboard

合伙人使用可重复分享的签名归因标识；`onShareAppMessage` 生成带安全参数的小程序路径，手动邀请码作为兜底。接收者注册成功后由后端永久绑定一个启用的合伙人，后续打开其他链接不得覆盖。分享动作本身不授予购买资格，仍须经过现有审核规则。

单页 Dashboard 至少展示：

- 当前分成规则、类型、规则版本、生效日期、佣金条件、退款冲正规则和预计结算周期；
- 累计佣金、待结算、已结算和可用金额；
- 分享按钮触发次数，并明确标注为行为事件；
- 通过链接或邀请码归因的去重注册人数；
- 已填资料、已审核、已购买会员人数；
- 注册转化率、付费转化率；
- 最近 7 天和 30 天的注册、付费、佣金趋势及简单漏斗。

“分享人数”以实际归因注册的去重人数为准，不能用分享按钮点击次数冒充实际人数。

建议数据边界：

```text
partner_commission_rule
partner_referral_attribution
partner_commission_ledger
partner_dashboard_daily（必要时再加）
```

佣金只能基于服务端验证的支付成功事件产生，使用订单号和幂等键防重；退款写反向台账，不直接改历史记录。前端不得计算钱。本轮 Dashboard 以只读展示为主，不实现复杂提现。

## 9. 人工客服

在投诉、支付异常、匹配异常和账号问题等适当位置接入微信官方客服：

```html
<button open-type="contact">联系人工客服</button>
```

不得把用户个人微信号明文写进代码、数据库、文档或配置。代码只完成官方客服入口和必要的会话/工单衔接；将指定微信账号添加为小程序客服人员属于微信后台人工配置，执行到该步骤时按项目级停止条件暂停并列出操作清单。

现有管理后台会话与工单审计能力继续保留，不在本轮扩张为复杂运营系统。

## 10. 本地优先实施顺序

1. 运行交接文档规定的六组基线 selfcheck，记录实际结果，不得通过回滚用户改动使测试变绿。
2. 定向检查现有未提交规格/代码，确认哪些能力已经实现，避免重复开发和推翻现有 UI。
3. 用测试驱动完成“其他补充需求”和两种 AI 意图交互模式。
4. 用并发测试驱动完成全局一次成功匹配的原子约束。
5. 完成硬筛、双向结构化评分、脱敏 Top-K 和 DeepSeek 严格 JSON 重排。
6. 完成综合分、进度条和双方独立报告。
7. 完成合伙人分享归因和单页 Dashboard。
8. 完成人工客服本地入口。
9. 验证本地小程序、云函数模拟、管理后台和合伙人后台。
10. 做安全审查、最终 diff 审查和分批提交。

至少覆盖：

```text
high_fit
medium_fit
edge_pass
hard_reject
missing_data
单向高分/另一方低分
地区或外貌气质自由描述
补充需求冲突
automatic/confirm 两种模式
AI 超时、非法 JSON、低置信度和确定性回退
并发争抢同一候选
已匹配用户再次发起或再次成为候选
邀请归因不可覆盖
普通用户越权访问合伙人接口
合伙人跨租户读取
佣金幂等与退款冲正
128 分制历史记录归一化
客服入口渲染
```

## 11. 整个 Project 的停止条件

### 11.1 成功停止

以下条件全部满足即可结束本轮，不继续扩大范围：

1. 本轮功能均已在本地实现；
2. 小程序相关逻辑、云函数模拟、管理后台和合伙人后台在相称范围内可运行；
3. 新增测试全部通过，六组基线 selfcheck 没有新增失败；
4. 两种意图模式都可切换和运行；
5. 并发重复匹配数为 0；
6. 综合分、报告、邀请归因、Dashboard 和客服入口验收通过；
7. DeepSeek 只通过服务端环境变量调用，模型不接触直接身份信息、不读写全库；
8. 最终 diff、安全和隐私审查通过；
9. 所有本轮修改已按独立批次提交并报告 commit hash。

### 11.2 外部配置停止

出现以下任一情况立即暂停，不得伪造成功：

- 需要用户填写 CloudBase 环境 ID、DeepSeek 生产密钥、AppID、AppSecret；
- 需要添加微信客服人员；
- 需要修改生产数据库权限；
- 需要配置支付商户信息、回调、合法域名、备案或证书；
- 需要部署生产云函数、上传体验版或正式版客户端；
- 需要操作真实用户数据或用户在微信/腾讯云后台确认权限。

暂停时报告：已完成内容、失败证据、需要用户填写/操作的项目、操作位置、填写后验证方法、当前分支和 commit hash。

### 11.3 CloudBase 与工具停止

1. CloudBase 管理和核验优先且仅在授权范围内使用 CloudBase MCP。
2. MCP 能完成的非破坏性操作可以继续；生产写入仍只能走项目白名单业务服务。
3. MCP 不存在、无权限或不支持时，可以检查项目已有的安全、非交互命令是否足以完成本地验证。
4. 如果只能通过浏览器人为操作，则立即停止。
5. 禁止使用 Computer Use、模拟鼠标键盘或浏览器自动化操作 CloudBase、微信公众平台或腾讯云控制台。
6. 明确区分部署 `api`/worker 云函数与上传微信小程序客户端；二者是独立发布动作，均需用户授权。

### 11.4 安全停止

出现以下任一情况立即暂停：

- 需要批量修改生产数据库；
- 需要删除、覆盖或混入用户已有改动；
- 需要把微信号、手机号、OpenID、密钥或私钥写进代码/文档；
- 现有并发改动与本任务无法安全拆分；
- 发现严重支付、权限或隐私问题且无法在本地安全验证；
- 需要执行不可逆生产操作或扩大到未授权范围。

### 11.5 防止无限循环

- 同一 AI 请求最多重试 1 次；
- 意图澄清最多 3 轮；
- 候选占用重试不超过 Top-K 数量；
- 同一失败测试没有代码修改或新证据时不得反复运行；
- 外部配置缺失或达到工具停止条件后不得反复尝试；
- 达到成功停止条件后不得继续追加非目标功能。

现在开始执行：先完成本地适配和测试，不操作生产环境，不使用 Computer Use 进入任何云端控制台。
