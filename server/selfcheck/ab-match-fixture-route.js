const assert = require('assert')
const fs = require('fs')
const path = require('path')

const root = path.resolve(__dirname, '../..')
const read = (file) => fs.readFileSync(path.join(root, file), 'utf8')
const backoffice = read('miniprogram/cloudfunctions/api/handlers/backoffice.js')
const match = read('miniprogram/cloudfunctions/api/handlers/match.js')
const db = read('miniprogram/cloudfunctions/api/lib/db.js')
const admin = read('server/public/admin/index.html')

assert(backoffice.includes("require('./abMatchFixture')"))
assert(backoffice.includes('/api/admin/users/(\\\\d+)/ab-match-fixture'))
assert(backoffice.includes('changeAbMatchFixture({'))
assert(db.includes('async function removeByDoc('))
assert(db.includes('removeByDoc'))
assert(match.includes("const abTestRunId = String(partner.ab_test_run_id || '')"))
assert(match.match(/ab_test_run_id:\s*abTestRunId/g).length >= 2)
assert(admin.includes('adminAbMatchFixture('))
assert(admin.includes('submitAdminAbMatchFixture('))
assert(admin.includes("memberApi('/admin/users/' + userId + '/ab-match-fixture'"))
assert(admin.includes('准备A/B测试'))
assert(admin.includes('清理A/B测试'))

console.log('PASS controlled A/B fixture route, tagging and admin UI contract')
