# AGENTS.md — WeFinally 执行边界

## 唯一工作目录

- 仓库根目录：`D:\wefinal\.worktrees\wefinally-ai-agent`
- 当前整合分支：`feature/ai-agent-system`
- 禁止在外层 `D:\wefinal` 直接开发。

当前工作树包含大量未提交和未跟踪的产品改动。未经用户明确授权，禁止执行 `git reset`、`git clean`、`git checkout`、`git restore`、提交、强推或覆盖其他人的改动。

## 每次接手先读

1. `PROJECT_HANDOFF.md`
2. `CONTRIBUTING.md`
3. `project-docs/NEXT_THREAD_HANDOFF_2026-07-26_MINIPROGRAM_CONTINUATION.md`
4. `project-docs/DEVELOPMENT_LOG.md`
5. 与任务直接相关的 `project-docs/MODULES/`、`project-docs/REQUIREMENTS.md` 和自检脚本

`project-docs/archive/` 只保存历史快照，不作为当前需求真源。

## 产品与安全边界

- 无头像、无用户私聊、无社交动态；官方客服协调线下见面。
- LBS 必须由用户主动授权，不后台静默采集；不宣称“直连 110”。
- Agent/模型不能扫描全库、直接写数据库或绕过确定性权限与安全规则。
- 不向模型发送手机号、OpenID、精确住址、单位、联系方式、密钥或私钥。
- 云端写入必须经过白名单业务服务；测试数据清理必须走有审计的后台业务按钮。
- 不依赖模型供应商 `conversation_id` 保存业务状态。
- 不直接批量修改生产数据库；表结构变更使用新迁移文件。
- 支付、会员、匹配和分润改动必须有专项自检与人工复核。

## 凭据

本地环境变量只保存在已忽略的 `.env` 文件或云端密钥配置中。文档、日志、截图、提交和聊天回复均不得记录真实密码、Token、API Key、微信支付 APIv3 密钥、商户私钥或管理员凭据。

历史提交曾包含本地测试凭据。接入 GitHub 前必须采用经确认的历史清理方案或建立经过审计的全新基线，不能直接推送现有历史。

## 基线验证

按顺序运行：

```powershell
npm --prefix server run selfcheck:agent
npm --prefix server run selfcheck:safety
npm --prefix server run selfcheck:ai-report
npm --prefix server run selfcheck:cloudpay
npm --prefix server run selfcheck:member
npm --prefix server run selfcheck:cloud-match
```

失败时依据实际证据修复，不能通过回滚现有改动“让测试变绿”。非平凡逻辑应保留可重复运行的自检，并更新 `project-docs/DEVELOPMENT_LOG.md`。

## 发布边界

“部署 `api` 云函数”和“上传小程序客户端”是两个独立动作。任何发布前都要记录源分支、提交号、自检结果和目标环境；未明确授权不得部署、上传或修改生产数据。

## 文件变更与 Git 收尾

- 每个会修改文件的实现任务，最终回复前必须产生一个 Git commit；纯只读核对或没有文件变化的任务不得创建空提交。
- 编辑前先运行 `git status`，把已经存在或并发出现的修改视为用户所有，禁止顺手整理、覆盖或打包提交。
- 每个开发批次使用独立任务分支；开始前记录基线分支和提交号，未经用户确认不得自动合并、推送、变基、改写历史或删除分支。
- 提交前审阅最终 diff，运行与风险相称的自检，并只暂存属于当前任务的文件或 hunk。
- commit message 应简短、可描述结果。仓库当前不存在 `main` 分支时，提交到用户指定的当前任务分支，不得为满足名称要求擅自新建或切换 `main`。
- 最终回复必须报告分支名、提交 hash、验证结果，以及仍未提交的用户改动；不得泄露提交中排除的凭据或隐私数据。

## 当前目标 Prompt：合伙人准入与 AI 双向匹配

下面的 Prompt 是当前开发目标。用户之后提供的新规则优先于本节；更新规则时必须保持安全边界、测试和可回退能力。

```text
你正在继续开发 WeFinally 微信小程序。实际工作目录只能使用
D:\wefinal\.worktrees\wefinally-ai-agent。

目标：完成“单一小程序、角色化界面、合伙人审核准入、其他补充需求参与 AI 双向匹配、综合匹配显示归一化分数”的本地可验证版本，并为 CloudBase 部署准备明确清单。

一、接手和 Git 规则
1. 完整阅读根目录 AGENTS.md、PROJECT_HANDOFF.md、CONTRIBUTING.md、当前交接文档和相关自检，禁止从头无差别扫描。
2. 编辑前运行 git status。已有或并发修改属于用户，禁止 reset、clean、restore、覆盖或捎带提交。
3. 在用户指定的任务分支工作；提交前审阅 diff、运行自检，只 stage 当前任务文件。
4. 每个会修改文件的实现任务都必须在最终回复前 commit；不要 push、amend、rebase、merge 或改写历史，除非用户明确要求。

二、产品形态：一个小程序，两种角色界面
1. 不维护两套小程序代码和两个版本包。使用同一个 AppID、同一个客户端，由后端可信身份决定普通会员或合伙人角色。
2. 普通会员只看到注册、资料、择偶配置、匹配、会员、客服等普通功能，不得看到合伙人入口。
3. 已启用合伙人看到独立“合伙人中心”，至少包含待审核用户和邀请分享；如果同一账号同时是会员，可在受控入口进入合伙人中心。
4. 前端隐藏不是权限控制。所有合伙人接口必须在云函数再次验证合伙人身份、状态和数据归属。
5. 合伙人使用可复用邀请入口一键分享多人。接收者打开分享卡后自动带入邀请归因，不要求手输邀请码；分享本身不授予购买资格。
6. 接收者必须真实注册、完整提交资料并由绑定合伙人或管理员审核通过，才能购买会员。
7. 分享参数不得包含手机号、OpenID、后台 Token、密钥或可直接枚举的内部凭据；注册归因、审核和资格变化必须可审计。

三、择偶配置：只增加“其他补充需求”
1. 不新增独立地区 picker 或单独“希望对方在哪个城市”字段。
2. 在择偶配置最底部、保存按钮之前增加“其他补充需求”多行文本框。提示用户可填写期望地区、未来城市安排、生活方式和结构化选项未覆盖的具体要求。
3. 第一版为选填，建议最多 500 字；客户端显示字数，云函数独立做 trim、类型和长度校验，拒绝未知写入字段。
4. 字段写入 user_match_setting，并进入会员申请审核快照；管理员和对应合伙人可以查看，另一位普通用户不能查看原文。
5. 历史记录缺失该字段时必须向后兼容，不得阻断匹配或审核。

四、AI 双向匹配
1. AI 匹配属于“匹配”模块，但不得改成纯 RAG 或让模型扫描全库。
2. 主链必须保持：确定性硬条件筛选 -> 双方独立结构化评分 -> 双向质量门槛 -> 脱敏 Top-K -> DeepSeek 语义重排 -> 后端复核和审计 -> 双方独立报告。
3. 年龄、性别、状态、黑名单、重复匹配、会员资格、安全规则和明确硬条件只能由确定性代码裁决；DeepSeek 不得推翻硬淘汰结果。
4. 对 Top-K 中每个候选，分别理解 A 的“其他补充需求”对 B 的满足程度，以及 B 的“其他补充需求”对 A 的满足程度。不能只做单向讨好，也不能把双方原文互相展示。
5. 发给 DeepSeek 的数据必须限长、脱敏和结构化，只包含匹配所需标签、粗粒度地区/生活安排、确定性分数摘要和补充需求安全摘要；不得包含手机号、OpenID、精确地址、单位、收入原文、密钥或真实数据库主键。
6. DeepSeek 必须返回严格 JSON：候选引用、A->B 语义分、B->A 语义分、综合语义分、证据标签、待确认点、风险标签和置信度。后端校验 schema、候选集合、取值范围和 Top-K 边界后才能使用。
7. AI 分数只能作为有上限的重排权重；确定性双向质量门槛仍是最终门卫。模型超时、密钥缺失、响应损坏或低置信度时，稳定回退到原确定性排序，不得让匹配整体失败。
8. RAG 仅用于客服知识库、规则和已审核话术检索，不把用户档案库做成 RAG 知识库。LangGraph 可管理客服或约会协调状态，但不是匹配算法本身。
9. 模型不能直接写数据库。匹配结果、审计和报告任务全部经过现有白名单业务服务，具备身份校验、幂等和可回退能力。

五、综合匹配分数显示
1. 匹配详情“综合匹配”进度条旁必须明确显示归一化百分比分数，例如“78分”或“78%”，进度条宽度与同一个归一化值一致。
2. 历史记录取值顺序固定为：score_detail.normalized_total -> score_detail.normalizedTotal -> total/max_total 计算 -> 最后才回退旧 total_score。
3. 禁止把 128 分制原始 total_score 直接当百分制；显示值限制为 0-100，并为 0、边界值、旧记录和缺失值写测试。
4. AI 重排可以解释“为什么排序变化”，但不得伪造或覆盖确定性综合分；如展示 AI 语义结果，应与综合分清楚区分。

六、本地优先和 CloudBase 暂停条件
1. 先为字段、接口、AI schema、双向语义、失败回退、角色权限、分享归因和分数显示写失败自检，再完成本地实现与回归。
2. 继续复用现有 CloudBase api 云函数、DeepSeek 安全边界、Top-K 重排策略和后台业务接口，不另起一个可绕过权限的后端。
3. 本地实现不得直接修改生产数据库。测试夹具只能使用合成数据，清理走有标记、可审计的业务接口。
4. 如果下一步必须在云开发控制台创建 collection/index/TTL、填写 DeepSeek 或 LangGraph 环境变量、配置 HTTP 访问、CORS、云函数 Node 运行时、上传小程序或部署云函数，先停止自动执行。
5. 暂停时输出精确人工操作清单：目标环境、资源名称、字段/索引、环境变量名（不含值）、预期作用、验证命令和回滚方式；等待用户完成后再继续云端验证。
6. 不得把“本地测试通过”描述为“CloudBase 已部署”，也不得把“api 云函数已部署”描述为“小程序客户端已上传”。

七、最低验收
1. 普通会员不能看到或调用合伙人功能；启用合伙人可以审核自己的邀请用户并一键分享。
2. 分享进入、自动归因、注册、资料提交、合伙人审核和购买资格形成可重复测试的闭环。
3. “其他补充需求”可保存、回显、进入审核快照，并能在脱敏后影响 Top-K 双向 AI 重排。
4. hard reject 永远不会被 AI 复活；DeepSeek 不可用时仍可完成确定性匹配。
5. 综合匹配进度条和显示分数使用同一个正确归一化值。
6. 六组基线自检、LangGraph 相关自检、AI 匹配专项自检和客户端静态检查通过；不能删除或跳过失败测试。
7. 最终报告列出改动文件、分支、commit hash、验证证据、CloudBase 待人工配置项和未覆盖的真机/体验版风险。
```
