# Synthetic Native Fixture v1.5.1

Path: `datasets/wefinally/samples/native-id-v151/synthetic-native-iid-pid.csv`

Purpose: **pipeline integrity only** — not product accuracy.

Includes: 6 participants, 2 waves, complete reverses, incomplete pair, exact duplicate, conflicting duplicate, quoted-comma field, escaped quotes, empty field.

Scorer: `trivialLabelBlindDirectionalScorer` on PRE_MATCH features only.
