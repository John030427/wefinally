# True Reverse Contract v1.5.1

- `p_ab` from A's subject FeatureView (`pid=B`)
- `p_ba` from B's subject FeatureView (`pid=A`)
- Never swap vectors / ages / synthetic reverse features
- Runtime: `TRUE_REVERSE_REQUIRES_NATIVE_SUBJECT_ROW`
- Feature view: PRE_MATCH only; gold access throws `GOLD_LABEL_ACCESS_FORBIDDEN`
