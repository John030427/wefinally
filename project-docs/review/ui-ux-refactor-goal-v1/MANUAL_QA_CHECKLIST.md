# MANUAL_QA_CHECKLIST — 编译 / 回归 / 走查

> 分支：`feature/ui-ux-refactor-goal-v1` · 检查方式：自动化静态检查（已执行）+ 人工走查（清单）
> 约束遵守：未部署生产 ✅ 未上传微信体验版 ✅ 未改云函数/后端 ✅

## 1. 自动化静态检查（已执行，全绿）

| 检查 | 方法 | 结果 |
|---|---|---|
| JS 语法 | `node --check` 全部 `miniprogram/**/*.js` | ✅ 0 失败 |
| JSON 有效 | `ConvertFrom-Json` 全部 `.json` | ✅ 通过（3 个 package-lock 为 PowerShell 解析限制，非真实问题，未改动） |
| WXSS 花括号平衡 | 正则统计 `{}` | ✅ 全部平衡 |
| WXML 标签配对 | `tools/check-wxml.js`（注释剔除+栈匹配） | ✅ `WXML OK: all tags balanced` |
| usingComponents 路径 | 逐页解析 json 并验证组件文件存在 | ✅ 0 问题 |
| app.json pages 完整性 | pages 列表 × 4 文件存在性 | ✅ 通过（`partner-login/partner-invite` 缺 `.json` 为基线既有状态，微信按默认配置处理，非本轮引入） |

## 2. 微信开发者工具编译（需人工一步）

- CLI 现状：`D:\微信web开发者工具\cli.bat` 存在，但 IDE **服务端口未开启**（安全设置），CLI 无法自动编译。开启属于用户本机 GUI 安全设置，任务未擅自更改。
- 人工验证步骤（约 1 分钟）：
  1. 打开微信开发者工具 → 设置 → 安全设置 → 开启服务端口
  2. 导入项目：`D:\wefinal\.worktrees\wefinally-ui-ux-refactor-goal-v1\miniprogram`
  3. 点编译，确认无报错；或 CLI：`& "D:\微信web开发者工具\cli.bat" open --project "<上述路径>"`
- 预期：无编译错误；首页/记录/详情/约会/聊天/我的均可打开

## 3. 业务回归走查清单（对照 AGENTS.md 安全边界）

- [ ] 首页：加载/无网/错误三态正常；journey 进度带 CTA 跳转正确；110 按钮仍触发广东110逻辑；VIP 卡两种状态正确
- [ ] 新匹配弹窗：有未读新结果时首开弹出；「查看匹配理由」进详情；「稍后再看」关闭且不再重复弹；无新结果不弹
- [ ] 匹配记录：列表加载正常；「新」徽标只标首条未见记录；点卡片进详情；通知入口跳转通知页
- [ ] 匹配详情：锁定态 VIP 引导正常；AI 报告生成中显示统一等待态且页面自动刷新；失败态有重试；反馈提交正常；算法细节展开/收起正常；底部「申请第一次约会」跳转协调页；安全确认跳转带参正确
- [ ] 约会协调：状态词胶囊与步骤条随 status 正确变化；发起建议→邀请→接受/协调/婉拒全链路文案与按钮同旧版一致；处理中显示 AI 等待态；失败可重新处理；转人工入口正常；表单提交逻辑未变
- [ ] AI 恋爱助手：入口页话题点击带 prompt 进 chat；chat 生成中/失败/重试正常；约会申请预览确认流正常（协调场景）；用户ID复制正常
- [ ] 我的页：四组菜单全部可点通；激活码兑换弹窗正常；合伙人工作台各状态跳转正常；退出登录正常
- [ ] AI 合规：chat/详情报告/协调页/恋爱助手入口 🤖 标识全部在位
- [ ] 隐私红线：无私聊入口、无头像社交、不展示三观原文（详情页 privacy note 保留）

## 4. 设计走查（wefinally-ui-design skill 评审清单）

- [x] 3 秒主线：首页三问（有无新匹配/下次何时/点哪）有明确答案
- [x] 决策型 CTA 唯一：详情页主 CTA=申请第一次约会；协调页按状态单一主按钮
- [x] AI 标识合规统一（紫系胶囊）
- [x] P0 页面无 emoji/线性图标混排（chat 消息内 💌📌 为内容标记，保留）
- [x] 颜色全部来自 token（新写 WXSS 无硬编码新色值；历史页面遗留色值属 P1/P2 范围）
- [x] 状态词符合统一词汇表
- [x] 触控目标 ≥ 88rpx（按钮 76–92rpx，列表行 ≥ 88rpx）
- [x] 业务逻辑与安全逻辑未动（diff 复核：仅展示层改动，详见 PAGE_BY_PAGE_CHANGES）

## 5. 截图

- 风格板：`style-board.png`（4 方向对比，已验证渲染）
- 小程序页面截图：需微信开发者工具（见 §2 人工步骤后补充）；本轮以静态检查 + 代码走查代替，未虚构截图
