# 自由约会地点修复与双真机验收

## 已修复及边界

发布工作树：`D:\wefinal\.worktrees\wefinally-release-20260904`。
地点允许商圈、商场、具体门店；纯菜品仍需补充一个见面地点。邀请不等于最终成约。
地点安排方式和未定事项在公开方案、邀请、偏好修改、重算与直接接受间保持；不采信客户端/模型的 accepted_by。
最终成约必须由服务端当前版本确认事务判定。更改地点安排方式会重新协调，旧确认不复用。

## 两部手机测试

上传本工作树的客户端测试版，两部手机更新到同一版本。此次不必清空历史资料或匹配。

1. A 填“万象城 / 吃饭 / 明确日期和20:00”，补完表单要求的预算等字段，发送；B 应看到原地点，不出现超出范围或 SERVER_ERROR。
2. A 选择“到场后再选店”。发送前和邀请中应显示未定事项，不能显示双方已确认；B 接受同一版本后才显示双方已确认。约定事项仍保留。
3. A 填“星巴克 / 电影”。允许发邀请但要提示先碰面还是活动场地；未澄清不能成约。通过电影对应选项或 AI 说明“先在星巴克碰面，影院到时商量”，查看修改预览并确认后，再由对方确认。
4. 已填具体电影院时说“星巴克只是集合点”，原电影院必须保留，只调整会合范围。若只是“对/可以”但上下文有多个候选，不得随意接受所有条件。
5. B 保留旧邀请页面，A 改为另一天或改变地点安排方式；B 点旧确认应提示刷新，不能确认旧版本。刷新后查看变化再确认。
6. 双方各自进入协调表单并选择同一方案：第一方确认后仍待另一方；第二方确认当前同一方案后才成约。选不同方案不得成约。
7. 地点只写“椰子鸡”时应引导补充商圈或门店；写“大运中心四季椰林”应保留。系统不会验证店铺真实营业情况。
8. 邀请/方案更新后的聊天卡应展示时间、地点和约定事项；另一手机重新打开会话检查是否收到更新。旧消息不追溯改写，新更新应采用新规则。

失败请保留：双方页面截图、操作时间、用户支持编号、协调编号（如页面可见）、客户端版本。不需要发送手机号、OpenID 或登录凭据。

## 自动化证据与限制

- `selfcheck:agent`：公开字段链路、邀请、预览、双边协调、事务版本校验与通知测试通过。
- `date-coordination-processing-store`：执行实际 db 事务函数，使用内存数据库替身验证旧确认/伪造 accepted_by 无效，以及冲突时回滚；不代表实际 CloudBase 事务压力测试。
- `agent-graph check`：构建及 42 项测试通过，包含 API/Graph 共享日期合约和 checkpoint 恢复。
- `safety / ai-report / cloudpay / member / cloud-match / qa-pair-reset / wx-identity`：通过。
- LIVE_GRAPH_SMOKE 和双真机体验仍需验收。云函数 health 成功不代表真实模型对所有自然语言均理解正确。
