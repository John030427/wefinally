# 模块 05：AI 匹配

> 详见 `AI_MATCHING_DESIGN.md`、`AI_MATCHING_RESEARCH.md`

## 代码位置

| 项 | 路径 |
|----|------|
| 定时任务 | `server/src/cron/matchCron.js` |
| 匹配引擎 | `server/src/services/matchService.js` |
| 三观算法 | `server/src/utils/viewSimilarity.js` |
| 记录表 | `user_match_log` |
| 展示 | `index`, `match-list`, `match-detail` |

## 当前逻辑

- 周三/五 0:00 cron
- 为**有效 VIP** 用户选 1 名候选
- 权重：婚育30 > 三观25 > 年龄15 > 身高12 > 学历8 > 圈层6 > 同城4
- 三观：Jaccard 字符 n-gram
- **问题**：候选要求 VIP；单向写 log

## 已确认变更（待开发 P1）

| 变更 | 说明 |
|------|------|
| 候选不要 VIP | 正常用户即可入池 |
| 双向互配 | 双向 scorePair + 对称 match_log |
| 算法 v1 | 够用，不接 LLM |
| 配置化 | 权重入 matchConfig.js |

## 完成度：PARTIAL

## 还缺什么

1. matchService 改造（双向 + 非 VIP 候选）
2. matchConfig.js
3. 非 VIP 被匹配展示策略（Q1）
4. 测试用例
5. 可选：match_score、match_role 字段

## 待确认

- Q1：非 VIP 看到什么

## 后续计划

1. R2 抽权重
2. feature/bidirectional-match 分支
3. 远期：LLM 理由（开关默认关）

## 不能随便动

- cron 周三/五节奏
- view_similarity 前台仅展示百分比、不展示原文
