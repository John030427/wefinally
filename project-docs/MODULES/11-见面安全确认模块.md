# 模块 11：见面安全（110 方案一）

> 来源升级：老板原话 A7 +《微信小程序对接深圳110系统的合规解决方案.docx》。
> ⚠️ **本模块范围已扩大**：老板明确要做「方案一安全功能」，含 **LBS 定位 + 紧急联系人 + 一键呼救**——**覆盖**早期 C2/Q5「v1 不做实时定位/紧急联系人/一键报警」的决定。

## 产品定位

- 仍是**官方客服对接奔现之后**的用户自我安全层，文案：**线下见面安全确认**。
- 分两阶段（老板原话）：
  - **方案一（现在做，完全合规，不直连公安）**：见面报备 + LBS 一键呼救（跳转官方「广东110」小程序）+ 平台内部预警。1–2 周可上线，无资质门槛。
  - **方案二（用户量大后再做）**：报警信息直推深圳公安接警台。需 ICP 证、等保二级、APP/公安备案、警企合作，3–6 个月。**v1 不做，仅留演进位**。

## 方案一 MVP 范围

### 做
1. **见面报备表单**：见面对象、**实时 LBS 定位 + 详细地址**、见面时间段、见面说明、**紧急联系人**（亲友手机号）。数据**加密留存**于自有服务器，作证据链。
2. 安全提示勾选「我已阅读线下见面安全提示」。
3. 报备状态（见面前/进行中/结束）；见面结束自动结束报备；中途可手动更新实时位置。
4. 历史记录列表。
5. **一键呼救（已定型 v1）**：
   - ① **`wx.makePhoneCall` 直拨 110** + 屏幕展示当前 LBS 定位/地址（供用户报给接警员）。零授权、永远可用。
   - ② **跳转「广东110」官方小程序**：需对方授权(appId + 跳转白名单)，**v1 拿不到**。代码留位：`safetyConfig.guangdong110 = { enabled:false, appId:'' }`，拿到授权后 `enabled:true` + 填 appId 即用 `navigateToMiniProgram` 启用，**不改其它代码**。
   - ③ **平台内部预警**：呼救即写一条 SOS 记录(定位/紧急联系人/时间，证据链) + 通知紧急联系人。⚠️ **暂无短信服务商、暂无 24h 客服人力** → v1 先记录 + 引导用户自行联系紧急联系人/转发安全卡；真实短信/推送/客服值守后接。
6. ~~见面安全卡（脱敏）+ 用户主动转发给本人好友/家人~~ → **已撤下(2026-06-29)**。安全卡是 C3(John 新增)、不在老板 110 文档里；老板明确「按照我的提示来，不要自己来」→ v1 不做，严格只对齐老板 110 方案一。安全卡降级为**待老板确认**项，他点头再加。（前端已移除卡片展示+转发；后端 `card_no` 字段保留无害）

### 不做（合规红线 + v1 范围）
- **不直连/不宣称"直连110出警系统"**（只能写「跳转广东公安官方报警小程序，同步实时定位」，虚假宣传会被处罚）。
- LBS **必须用户主动授权**，仅见面报备/呼救场景用，**禁止后台静默持续采集**。
- 用户端上传照片、复杂风控模型、默认展示手机/身份证/真实姓名。
- 安全卡不在小程序内发给匹配对象、不公开展示、v1 不做存相册（沿用 Q5）。
- 见面报备 ≠ 平台免责（仍需实名/黑名单/杀猪盘风控）。

## 见面安全卡字段
脱敏对象标识、预计时间、预计地点、见面说明、安全提示、小程序名、生成时间、安全卡编号。

## 代码位置（规划）
| 层 | 路径 |
|----|------|
| 新页-表单 | `miniprogram/pages/meet-safety/` |
| 新页-历史 | `miniprogram/pages/meet-safety-list/` |
| 入口 | `match-detail` `.meet-card` + `profile.menuList` |
| 配置 | `server/src/config/safetyConfig.js`（开关/文案/字数/广东110跳转配置） |
| 表 | `meet_report`（规划，新增 lat/lng/address/emergency_contact 等字段） |
| API | `server/src/routes/meet.js` |

## UI 复用
表单：register picker + match-setting textarea + agreement checkbox；列表：match-list；卡片/说明：match-detail .card / marry-report .notice-card。一键呼救按钮单独醒目。

## 完成度：0%

## 待确认（QUESTIONS）
- M-1：方案一 v1 是否**全做**（报备+LBS+紧急联系人+一键呼救+内部预警），还是先做「报备+安全卡」、呼救/LBS 二期？（老板说"加上方案一"，倾向全做，但工作量更大，需 John 拍节奏）
- M-2：「广东110」官方小程序的 **AppID / 跳转路径**需实测确认（微信 `navigateToMiniProgram` 需配置且对方需允许被跳转）。
- M-3：紧急联系人是必填还是选填？呼救通知客服的渠道（短信/站内/人工值班）？24h 安全客服是否有人力？
- M-4：LBS 授权与隐私协议条款需补（个保法）。

## 后续计划
1. safetyConfig + meet_report migration（含定位/紧急联系人字段）
2. meet-safety 表单页 + match-detail/profile 入口
3. 一键呼救（跳广东110 + 内部预警）+ 安全卡分享
4. meet-safety-list 历史
5. （远期）方案二警企直推——先攒资质
