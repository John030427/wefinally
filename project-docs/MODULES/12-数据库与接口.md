# 模块 12：数据库与接口

## Schema 真源

**唯一建表文件**：`database/init.sql`  
**勿用**：`server/migrations/001_schema.sql`（已 DEPRECATED）

补丁：`database/patch-002-partner-audit.sql`（仅 `partner_user_audit_log`，该表 init.sql 已含，补丁只用于升级旧库）

## 现有 15 表

init.sql 内有 15 张 `CREATE TABLE`（表注释编号至 14，第 15 张 `partner_user_audit_log` 接在 FAQ 种子后未编号，见 init.sql:279）。明细见 `PROJECT_STRUCTURE.md` 第四节。

## 规划新增

| 表/字段 | 用途 |
|---------|------|
| `user.appearance_description` | 外貌描述 |
| `meet_report` | 见面安全确认 |

### meet_report 草案

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT PK | |
| user_id | INT | 发起人 |
| match_user_id | INT | 见面对象 |
| meet_time | DATETIME | 预计时间 |
| meet_place | VARCHAR | 地点 |
| meet_note | TEXT | 说明 |
| safety_ack | TINYINT | 是否勾选安全提示 |
| status | TINYINT | 0草稿 1已确认 2已取消 3已过期 |
| card_no | VARCHAR | 安全卡编号 |
| created_at | DATETIME | |

## API 前缀

| 前缀 | 路由文件 |
|------|----------|
| `/api/auth` | auth.js |
| `/api/user` | user.js |
| `/api/match` | match.js |
| `/api/order`, `/api/vip` | order.js |
| `/api/chat` | chat.js |
| `/api/report` | report.js |
| `/api/common`, `/api/platform` | common.js |
| `/api/partner` | partner.js |
| `/api/admin` | admin.js |
| `/api/wxpay` | wxpay.js |

## 规划新增接口

```
GET  /api/common/config          # 配置下发
PUT  /api/user/profile           # 含 appearance_description
GET  /api/meet/list
POST /api/meet/create
GET  /api/meet/:id
POST /api/meet/:id/cancel
```

## 响应格式

```json
{ "code": 0, "message": "ok", "data": {} }
```

认证：`Authorization: Bearer <token>`

## 文档

- 现有：`docs/API-REFERENCE.md`
- 新接口实施后需追加

## 完成度：~90%

## 后续

- P1：config API
- P2：meet_report migration + routes
