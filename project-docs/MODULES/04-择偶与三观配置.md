# 模块 04：择偶与三观配置

## 代码位置

| 项 | 路径 |
|----|------|
| 择偶页 | `miniprogram/pages/match-setting/` |
| 用户侧 API | `server/src/routes/user.js` match-settings |
| 匹配侧 API | `server/src/routes/match.js` setting |
| 冷却 | `guard.js` + `MATCH_COOLDOWN_DAYS=7` |

## 功能说明

- 6 维下拉：年龄、身高、学历、城市、婚姻偏好、婚育节奏
- 双三观文本：【我的三观自述】【期待对方三观】，20-300 字
- 7 天冷却：前后端强制，可视化倒计时

## 完成度：DONE（有小问题）

| 项 | 状态 |
|----|------|
| UI + 校验 | DONE |
| 服务端冷却 | DONE |
| 重复 API | 两套路由并存（`/api/user/match-settings` PUT + `/api/match/setting` POST） |
| 三观必填 | **已确认选填**：空值放行、填了才校验 20–300 字 |

## 已确认产品规则（2026-06-29 · B-Q1）

- 三观文本**不强制必填**（守护 PRD 极简注册原则）
- 但**一旦填写，仍校验 20–300 字**（保证匹配质量）
- 代价提示：三观是匹配**第 2 权重（25 分）**，留空者匹配质量下降；前端应保留**强软引导**

## 还缺什么

1. 统一 `/api/user/match-settings` 与 `/api/match/setting`（R4）
2. 三观保持**选填**（B-Q1 已确认）：核对前后端均为"空值放行、非空才校验 20–300 字"（后端当前已允许空，需确认非空时的字数校验生效）

## 后续计划

- 保持 UI 不动，仅后端收敛
- 权重在 matchService 使用本模块数据

## 不能随便动

- cooldown-banner 与保存禁用逻辑
- 三观字数校验 UX
