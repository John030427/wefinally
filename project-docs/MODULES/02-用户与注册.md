# 模块 02：用户与注册

## 代码位置

| 页面/接口 | 路径 |
|-----------|------|
| welcome | `miniprogram/pages/welcome/` |
| login | `miniprogram/pages/login/` |
| agreement | `miniprogram/pages/agreement/` |
| register | `miniprogram/pages/register/` |
| 微信登录 | `server/src/routes/auth.js` |
| 注册 | `server/src/routes/user.js` POST register |

## 功能说明

欢迎 → 微信登录 → 三协议勾选 → 全下拉注册（含推广码静默绑定）→ 合伙人审核

## 完成度：~85%

| 项 | 状态 |
|----|------|
| 登录/协议/注册 UI | DONE |
| 隐私授权日志 | DONE |
| 推广码归属 | DONE |
| 身高区间 | **待开发**（现为精确 cm，已确认立即改为区间，见下 Q3） |

## 已确认产品规则（2026-06-29）

| 规则 | 结论 | 来源 |
|------|------|------|
| **注册身高立即改区间档位** | register 用 `HEIGHT_RANGE_OPTIONS` 替换 150–200cm 精确选择 | Q3 |
| **旧数据兼容迁移** | 已存的精确身高（如 `175cm`）需迁移成区间（`170-180cm`），**不破坏历史数据** | Q3 |

## 还缺什么

1. **register 身高改区间**（Q3，立即做）：前端 `initHeights()` 改为读 `HEIGHT_RANGE_OPTIONS`
2. **旧精确身高迁移**（Q3）：需**新写分桶逻辑**——注意后端现有 `normalizeHeightRange()`（user.js:44）只是给数字补 `cm`，**不分桶**，不能直接复用
3. ⚠️ **连带影响**：matchService 的 `parseHeightCm()` 取"第一个数字"，区间化后 `170-180cm` 会被当成 170（下限）参与打分；迁移时建议改用**区间中位数**，避免老用户匹配分漂移
4. welcome 可选补网络状态

## 待确认

- 无（Q3 已确认：立即改 + 兼容迁移）

## 后续计划

- 独立分支 `fix/height-range`（含：register 前端改动 + 迁移脚本 + matchService 取数改中位数 + 专项验证）
- 迁移脚本入 `database/`（新 patch），不直接改 init.sql 已上线结构

## 不能随便动

- agreement 三勾选合规逻辑
- login token 存储流程
